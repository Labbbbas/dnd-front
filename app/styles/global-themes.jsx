"use client";

import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      // Blue
      main: "#005682",
    },
    secondary: {
      // Red
      main: "#dc004e",
    },
  },

  typography: {
    fontFamily: "Arial, sans-serif",
  },
});
